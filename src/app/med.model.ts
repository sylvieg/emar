export class Med {
    public id: number;
    public name: string;
    public strength: string;

    constructor(id: number, name: string, strength: string) {
        this.id = id;
        this.name = name;
        this.strength = strength;
    }
}